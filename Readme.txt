You must enable package downloading in vs.net. This can be found in Tools > Options > Package Manager > Allow Nuget to download missing packages during build.

You must run build from the command line or double click on the file clicktobuild.bat

This will get the packages from nuget and you will be able to build from vs.net.

